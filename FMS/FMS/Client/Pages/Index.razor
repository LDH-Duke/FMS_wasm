@page "/"
@using FMS.Client.Commons
@using FMS.Shared.Model
@inject HttpClient Http
@inject IJSRuntime JS

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />

<input type="text" @bind-value="PassWord" />

<input type="button" value="check" @onclick="btnCheck"/>

@code{
    UsersComm comm;
    private string PassWord { get; set; }

    protected override async Task OnInitializedAsync()
    {
        comm = new UsersComm();
        //var temp = await Http.GetFromJsonAsync<List<UserInfo>>("api/Users");
        //Console.WriteLine("");


    }

    protected void btnCheck()
    {
        bool temp = comm.CheckPassWord(PassWord);
        JS.InvokeVoidAsync("alert", temp);
    }
}